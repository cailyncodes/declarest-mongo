uri: 'mongodb://st:st@ds049624.mlab.com:49624/coledev'
routes:
  - /entries:
      method: GET # optional (default GET)
      collection: entries # required
      filter: ~ # optional (default ~)
  - /entries/draft:
      collection: entries
      filter:
        - meta.stage: Draft
  - /entries/internal:
      collection: entries
      filter:
        - meta.stage: Internal
  - /entries/public:
      collection: entries
      filter:
        - meta.stage: Public
  - /entries:
      method: POST
      collection: entries # required
      prepost: './examples/prepost.js'
