uri: 'mongodb://st:st@ds049624.mlab.com:49624/coledev'
routes:
  - /users:
      method: GET # optional (default GET)
      collection: users # required
      filter: ~ # optional (default ~)
  - /users/me:
      method: GET # optional (default GET)
      collection: users # required
      filter:
        - username: chansen
